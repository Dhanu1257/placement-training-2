class Solution:
    def moveZeroes(self, nums):
        k=0
        for x in nums:
            if x!=0:
                nums[k]=x
                k+=1
        for i in range(k,len(nums)):
            nums[i]=0
