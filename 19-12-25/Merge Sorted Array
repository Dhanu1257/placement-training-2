class Solution:
    def merge(self, a, m, b, n):
        i=m+n-1;m-=1;n-=1
        while n>=0:
            if m>=0 and a[m]>b[n]:
                a[i]=a[m];m-=1
            else:
                a[i]=b[n];n-=1
            i-=1
